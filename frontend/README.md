# Frontend

Interactive React + TypeScript UI for the semantic movie recommender.

## Run locally

```bash
cd frontend
npm install
npm run dev
```

Backend must be running at `http://127.0.0.1:5000`.

To use a different backend URL:

```bash
VITE_API_BASE_URL=http://localhost:5000 npm run dev
```
